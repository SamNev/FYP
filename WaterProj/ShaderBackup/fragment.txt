#version 130

uniform float u_MaxHeight;
uniform vec3 u_Color;

in VS_OUT {
    vec3 FragPos;
} fs_in;

void main()
{
    vec3 col = fs_in.FragPos.y > 0.0 ? u_Color * (0.1 + (fs_in.FragPos.y/u_MaxHeight) * 0.9) : vec3(0.0, 0.0, 0.3 -(fs_in.FragPos.y));
    gl_FragColor = vec4(col, 1.0);
}
