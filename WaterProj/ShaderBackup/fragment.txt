#version 130
in VS_OUT {
    vec3 FragPos;
} fs_in;

void main()
{
    vec3 col = fs_in.FragPos.y > 0.0 ? vec3(0.0, fs_in.FragPos.y/3.0, 0.0) : vec3(0.0, 0.0, 0.3 -(fs_in.FragPos.y));
    gl_FragColor = vec4(col, 1.0);
}
